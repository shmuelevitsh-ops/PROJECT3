# Region D, full extent: the open hallway approach + corridor gap + the
# walled room itself (real benchmark-house data).
mission_config:
  max_steps: 2000
  boundaries:
    x_boundary:
      min_cm: 170
      max_cm: 270
    y_boundary:
      min_cm: 190
      max_cm: 250
    height_boundary:
      min_cm: 210
      max_cm: 270
  gps_resolution_cm: 10
  output_mapping_resolution_factor: 1
