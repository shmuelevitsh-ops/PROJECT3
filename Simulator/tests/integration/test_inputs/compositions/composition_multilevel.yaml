# Dedicated, minimal composition for Region C, a real multi-level,
# floor-crossing shaft. Kept to one run for CLI output-layout tests.
simulation_compositions:
  simulations:
    - simulation_config: "../simulations/sim_3.yaml"
      mission_configs:
        - "../missions/mission_d.yaml"

  drone_configs:
    - "../drone_configs/drone_small.yaml"

  lidar_configs:
    - "../lidar_configs/lidar_a.yaml"
