# Dedicated, minimal composition for a single full-scale run over the real
# benchmark house. Cross-product coverage is exercised by composition_basic.yaml.
simulation_compositions:
  simulations:
    - simulation_config: "../simulations/sim_full_house.yaml"
      mission_configs:
        - "../missions/mission_full_house.yaml"

  drone_configs:
    - "../drone_configs/drone_small.yaml"

  lidar_configs:
    - "../lidar_configs/lidar_full_house.yaml"
